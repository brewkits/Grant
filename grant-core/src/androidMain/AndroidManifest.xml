<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Transparent Activity for handling grant requests

        This activity is completely invisible to the user and handles
        runtime grant requests from ViewModels without requiring
        Fragment or Activity context in app code.

        Key features:
        - Zero visual impact (fully transparent)
        - No entry in recent apps
        - No back stack history
        - Handles configuration changes gracefully
        - Works from anywhere (ViewModel, Repository, etc.)
    -->

    <!--
        Backup Rules:

        Grant library uses SharedPreferences to track "has requested permission X"
        to solve Android's "Dead Click" issue after app restart.

        However, we MUST exclude this from backup to prevent false state after
        app reinstall:
        - If backed up: Reinstalled app thinks permission was requested before
        - But system sees: Fresh install (permission never requested)
        - Result: Inconsistent state causes UX issues

        Solution: Exclude from backup/restore so each install starts fresh.

        Note: App developers can override these rules in their app's manifest
        if they need different backup behavior.
    -->
    <!--
        This library deliberately declares NO <uses-permission>.

        Manifest merger copies a library's uses-permission entries into every consuming
        app, so a permission declared here appears on the app's Play Store listing and in
        every security audit — for a permission the app never asked for. A permission
        library injecting permissions is precisely the problem this library exists to
        solve, so the count here stays at zero.

        Removed in 2.4.1:
        - VIBRATE: never used by production code; it was only ever a harmless dummy
          permission in the instrumented tests, which now declare it themselves.
        - ACCESS_WIFI_STATE: needed only by the optional ServiceType.WIFI check
          (WifiManager.isWifiEnabled). Apps that use that check declare it themselves;
          see docs. checkWifiService() already catches SecurityException and reports
          ServiceStatus.UNKNOWN, so an app that does not declare it degrades rather
          than crashes.
    -->

    <!--
        On android:stateNotNeeded="true" alongside GrantRequestViewModel's SavedStateHandle,
        which looks contradictory but is not:

        - Across a CONFIGURATION CHANGE the ViewModel is retained in memory, so requestId and
          alreadyLaunched survive without onSaveInstanceState being involved at all. This is
          the case that actually matters (rotating the device while the system dialog is up).
        - Across PROCESS DEATH nothing can be restored anyway: the pending CompletableDeferred
          lives in a static map that dies with the process, and the coroutine awaiting it is
          gone too. onCreate() therefore falls back to the intent extra, which does survive,
          purely so the restored Activity can finish cleanly instead of hanging.

        stateNotNeeded="true" tells the OS not to spend effort preserving state for a
        transparent, short-lived dialog host whose state cannot be meaningfully restored. It
        does not weaken the configuration-change path.
    -->
    <application>
        <activity
            android:name="dev.brewkits.grant.impl.GrantRequestActivity"
            android:theme="@style/Theme.Transparent"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:taskAffinity=""
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:stateNotNeeded="true"
            android:windowSoftInputMode="stateHidden|adjustNothing" />

        <!--
            Registers foreground-Activity tracking before Application.onCreate() or any
            Activity is created (see GrantInitializer KDoc). authorities must be unique
            per-app, so it's namespaced with ${applicationId} like AndroidX Startup does.
        -->
        <provider
            android:name="dev.brewkits.grant.impl.GrantInitializer"
            android:authorities="${applicationId}.grantinitializer"
            android:exported="false"
            android:initOrder="100" />
    </application>

</manifest>
